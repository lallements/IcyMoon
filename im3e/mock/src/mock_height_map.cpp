#include "mock_height_map.h"

using namespace im3e;

MockHeightMapTileSampler::MockHeightMapTileSampler() = default;
MockHeightMapTileSampler::~MockHeightMapTileSampler() = default;

MockHeightMap::MockHeightMap() = default;
MockHeightMap::~MockHeightMap() = default;